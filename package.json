{
  "name": "stripe-nodejs",
  "version": "1.0.0",
  "description": "testing out the stripe api",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Daniel Ohn",
  "license": "ISC",
  "devDependencies": {
    "dotenv": "^8.2.0",
    "prettier": "^2.0.5"
  },
  "dependencies": {
    "stripe": "^8.49.0"
  }
}
